{
  "navigationFallback": {
    "rewrite": "/index.html"
  },
  "routes": [
    {
      "route": "/api/*",
      "methods": [ "GET", "POST", "PATCH", "DELETE" ],
      "allow": [ "anonymous" ]
    }
  ]
}
